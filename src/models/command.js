module.exports = {
    name: 'init',
    alias: [''],
    run: async (toolbox) => {
        const {
            parameters,
            template: { generate },
            print: { info },
            prompt,
            filesystem
          } = toolbox;


    }
};